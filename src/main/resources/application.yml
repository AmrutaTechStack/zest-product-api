spring:
  datasource:
    driver-class-name: oracle.jdbc.OracleDriver
    url: jdbc:oracle:thin:@localhost:1521:ORCL
    username: scott
    password: channel

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8080
